name: Test

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build the Docker image and run tests
      run: |
        docker-compose run app npm install nyc mocha --save-dev
        docker-compose run app npm test
